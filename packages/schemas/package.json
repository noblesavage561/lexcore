{
  "name": "@lexcore/schemas",
  "version": "0.1.0",
  "description": "Canonical JSON Schemas — single source of truth for all LexCore data contracts",
  "main": "index.js",
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "node scripts/validate.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
